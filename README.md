# ckcljs

# Usage

```
$ git clone $REPO
$ cd $REPO
$ lein install
$ cd ..
$ cd lein new ckcljs $APPNAME
$ cd $APPNAME

...in 3 separate windows...
$ lein cljsbuild auto
$ lein figwheel
$ lein ring server
```
